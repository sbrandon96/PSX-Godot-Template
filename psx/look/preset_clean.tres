; PSXLook preset: clean
; Modern rendering with only a distance fade. The control case: switch to this to see what the PSX effects are actually doing.
; Every value is written out explicitly, even where it matches the script
; default, so this file states the whole look rather than inheriting half of it.
[gd_resource type="Resource" script_class="PSXLookPreset" format=3 uid="uid://y3s3qedvyq7a"]

[ext_resource type="Script" uid="uid://fnrn61tu7upx" path="res://psx/look/psx_look_preset.gd" id="1_preset"]

[resource]
script = ExtResource("1_preset")
jitter_enabled = false
snap_resolution = Vector2(320, 240)
affine_strength = 0.0
color_depth = 256.0
dither_strength = 0.0
fog_enabled = true
fog_color = Color(0.06, 0.07, 0.09, 1)
fog_near = 20.0
fog_far = 60.0
